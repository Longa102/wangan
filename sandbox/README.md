# 受控 Agent 测试沙箱

这个目录只用于真实 Agent 与 wangan 安全代理的本地集成演示。

- `fixtures/`：可安全读取的测试文件；
- `output/`：受控 MCP 允许写入的唯一位置；
- 所有路径必须相对于本目录，例如 `fixtures/README.md`；
- 上游 MCP 永不执行命令，也不会进行任何网络请求。

请不要在这里放置真实密钥、客户数据或生产文件。
